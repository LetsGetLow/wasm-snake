{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup": "bun build:rs && bun install",
    "clean": "rm -rf dist node_modules ../wasm",
    "dev": "bun build:rs && vite",
    "build": "bun build:rs && tsc && vite build",
    "build:rs": "wasm-pack build --release --target web --out-dir ./wasm",
    "preview": "vite preview"
  },
  "devDependencies": {
    "snake-wasm": "file:../wasm",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.15",
    "@types/bun": "latest"
  }
}
