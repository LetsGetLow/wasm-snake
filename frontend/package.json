{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:rs": "wasm-pack build --target web --out-dir ./wasm",
    "preview": "vite preview"
  },
  "dependencies": {
    "snake-wasm": "file:../wasm"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.15",
    "@types/bun": "latest"
  }
}
